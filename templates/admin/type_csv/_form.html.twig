{{ form_start(form) }}
        
    <h4>Ajoutez des champs à lire dans le fichier csv :</h4>
    <ul class="tags list-group mb-3" data-prototype="{{ form_widget(form.champs.vars.prototype)|e('html_attr') }}">
    </ul>
    
    <button class="btn btn-sm m-1 btn-secondary">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}