{% extends 'admin/dashboard.html.twig' %}

{% block title %}Admin - Ajout d'une promotion{% endblock %}


{% block main %}
    <div class="col-lg-6">


        {{ form_start(promoForm) }}
            <div class="row">
                {{ form_row(promoForm.anneeDebut) }}
                {{ form_row(promoForm.anneeFin) }}
            </div>
            <div class="row">
            <button type="submit" class="btn btn-primary">Suivant <i class="fas fa-arrow-right"></i></button>
        {{ form_end(promoForm) }}
        <a href="{{ path('admin_liste_promotions') }}" class="btn btn-secondary" role="button" aria-pressed="true"><i class="fas fa-arrow-left"></i> Retour</a>
        </div>
    </div>
{% endblock %}