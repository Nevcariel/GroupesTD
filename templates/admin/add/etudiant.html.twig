{% extends 'admin/dashboard.html.twig' %}

{% block title %}Admin - Ajouter un étudiant{% endblock %}


{% block main %}
    <div class="col-lg-6 mb-3">
        {{ form_start(etudiantForm) }}
            {{ form_row(etudiantForm) }}
            <div class="row">
            <button type="submit" class="btn btn-primary"><i class="fas fa-save"></i> Sauvegarder</button>
        {{ form_end(etudiantForm) }}
        <a href="{{ path('admin_liste_promotions') }}" class="btn btn-secondary" role="button" aria-pressed="true"><i class="fas fa-arrow-left"></i> Retour</a>
        </div>
    </div>
    
{% endblock %}