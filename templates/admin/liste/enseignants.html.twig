{% extends 'admin/dashboard.html.twig' %}

{% block title %}Admin - Liste des enseignants{% endblock %}

{% block main %}
<div class="col-lg mt-3">
    <h3>Liste des enseignants</h3>
    <table class="table">
        <thead class="thead-light">
            <tr>
                <th>Prénom</th>
                <th>Nom</th>
                <th>Entreprise</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for enseignant in enseignants %}
            <tr>
                <td>{{ enseignant.getPrenom() }}</td>
                <td>{{ enseignant.getNom() }}</td>
                <td>{{ enseignant.getEntreprise() }}</td>
                <td><a href="{{ path('admin_delete_enseignant', {'enseignant': enseignant.getId()}) }}" class="offset-lg-2 btn btn-danger btn-sm" role="button" aria-pressed="true" onclick="return confirm('Veuillez confirmer la suppression.')"><i class="fas fa-trash"></i> Supprimer</a></td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
</div>
<a href="{{ path('admin_add_enseignant') }}" class="offset-lg-2 btn btn-secondary btn-sm" role="button" aria-pressed="true"><i class="fas fa-plus"></i> Ajouter</a>
{% endblock %}