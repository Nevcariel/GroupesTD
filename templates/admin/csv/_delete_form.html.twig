<form method="post" action="{{ path('admin_csv_delete', {'id': csv.id}) }}" onsubmit="return confirm('Confirmer la suppression');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ csv.id) }}">
    <button class="btn btn-danger btn-sm m-1"><i class="fas fa-trash"></i></button>
</form>