{% extends 'admin/dashboard.html.twig' %}

{% block title %}Association Bdd Csv{% endblock %}

{% block main %}
<div class="col-lg mt-3">
    <h1>Association Bdd Csv</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ association_bdd_csv.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_association_bdd_csv_edit', {'id': association_bdd_csv.id}) }}" class="btn btn-secondary btn-sm m-1" role="button" aria-pressed="true"><i class="fas fa-pencil-alt"></i></a>

    {{ include('admin/association_bdd_csv/_delete_form.html.twig') }}

    <a href="{{ path('admin_association_bdd_csv_index') }}" class="btn btn-secondary btn-sm m-1" role="button" aria-pressed="true"><i class="fas fa-arrow-left"></i> retour</a>

</div>
{% endblock %}